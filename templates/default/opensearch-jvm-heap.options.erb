# Generated by Chef - Local modifications will be overwritten
#
# OpenSearch's own package-installed jvm.options already covers GC and
# other JVM tuning; this drop-in only overrides heap size, per the
# jvm.options.d convention OpenSearch (like Elasticsearch before it) reads
# on startup.
-Xms<%= @heap_size %>
-Xmx<%= @heap_size %>
