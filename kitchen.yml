---
driver:
  name: <%= ENV.fetch('KITCHEN_DRIVER', 'vagrant') %>

provisioner:
  name: chef_zero
  chef_license: accept-no-persist

verifier:
  name: inspec

platforms:
  # AxonOps packages call systemctl in their postinst scripts. kitchen-docker
  # runs `sshd -D` as PID 1 by default (no real init system at all), so those
  # calls fail hard: "Failed to connect to bus: Host is down". The
  # axonops-systemd-* images (built by test.yml before `kitchen test`, from
  # test/docker/Dockerfile.systemd-*) add systemd on top of the stock
  # ubuntu/rockylinux base; overriding run_command to /sbin/init boots real
  # systemd as PID 1 instead, with privileged mode + the cgroup/tmpfs mounts
  # systemd needs to run inside a container at all. Only meaningful for the
  # docker driver — vagrant (a real VM) already has a normal init system.
  - name: ubuntu-22.04
    driver:
      <% if ENV.fetch('KITCHEN_DRIVER', 'vagrant') == 'docker' %>
      image: axonops-systemd-ubuntu-2204
      run_command: /sbin/init
      privileged: true
      run_options: --cgroupns=host -v /sys/fs/cgroup:/sys/fs/cgroup:rw --tmpfs /run --tmpfs /run/lock
      <% end %>
  - name: rockylinux-9
    driver:
      <% if ENV.fetch('KITCHEN_DRIVER', 'vagrant') == 'docker' %>
      image: axonops-systemd-rockylinux-9
      run_command: /sbin/init
      privileged: true
      run_options: --cgroupns=host -v /sys/fs/cgroup:/sys/fs/cgroup:rw --tmpfs /run --tmpfs /run/lock
      <% end %>

suites:
  - name: cassandra-311-tar
    run_list:
      - recipe[axonops::cassandra]
    verifier:
      inspec_tests:
        - test/integration/cassandra
    attributes:
      axonops:
        # axon-cassandra3.11-agent-1.0.4-1 (RPM) conflicts with axon-agent on
        # /var/lib/axonops' directory mode at install time — an AxonOps
        # package build defect (verified directly: `dnf install` fails with
        # "file /var/lib/axonops conflicts between attempted installs of
        # axon-cassandra3.11-agent... and axon-agent..."), not something
        # fixable from this cookbook. This suite exists to validate the
        # Cassandra 3.11 install/config path, not agent packaging, so
        # disabling the agent here avoids an unrelated external blocker.
        agent:
          enabled: false
        cassandra:
          version: '3.11.17'
          install_format: 'tar'
          start_on_install: false
          wait_for_start: false

  - name: cassandra-311-tar-rhel
    run_list:
      - recipe[axonops::cassandra]
    verifier:
      inspec_tests:
        - test/integration/cassandra
    includes:
      - rockylinux-9
    attributes:
      axonops:
        cassandra:
          version: '3.11.17'
          install_format: 'tar'
          start_on_install: false
          wait_for_start: false

  - name: cassandra-41-tar
    run_list:
      - recipe[axonops::cassandra]
    verifier:
      inspec_tests:
        - test/integration/cassandra
    attributes:
      axonops:
        cassandra:
          version: '4.1.7'
          install_format: 'tar'
          start_on_install: false
          wait_for_start: false

  - name: cassandra-50-default
    run_list:
      - recipe[axonops::cassandra]
    verifier:
      inspec_tests:
        - test/integration/cassandra
    attributes:
      axonops:
        cassandra:
          version: '5.0.5'
          install_format: 'tar'
          start_on_install: false
          wait_for_start: false

  - name: cassandra-50-pkg-debian
    run_list:
      - recipe[axonops::cassandra]
    verifier:
      inspec_tests:
        - test/integration/cassandra
    includes:
      - ubuntu-22.04
    attributes:
      axonops:
        cassandra:
          version: '5.0.5'
          install_format: 'pkg'
          start_on_install: false
          wait_for_start: false

  - name: cassandra-50-g1gc
    run_list:
      - recipe[axonops::cassandra]
    verifier:
      inspec_tests:
        - test/integration/cassandra
    attributes:
      axonops:
        cassandra:
          version: '5.0.5'
          install_format: 'tar'
          gc_type: 'G1GC'
          start_on_install: false
          wait_for_start: false

  - name: cassandra-50-shenandoah
    run_list:
      - recipe[axonops::cassandra]
    verifier:
      inspec_tests:
        - test/integration/cassandra
    attributes:
      axonops:
        cassandra:
          version: '5.0.5'
          install_format: 'tar'
          gc_type: 'Shenandoah'
          start_on_install: false
          wait_for_start: false

  - name: cassandra-41-pem-tls
    run_list:
      - recipe[axonops::cassandra]
    verifier:
      inspec_tests:
        - test/integration/cassandra
    attributes:
      axonops:
        cassandra:
          version: '4.1.7'
          install_format: 'tar'
          tls:
            internode_encryption: 'all'
            type: 'pem'
          start_on_install: false
          wait_for_start: false

  - name: cassandra-50-jks-tls
    run_list:
      - recipe[axonops::cassandra]
    verifier:
      inspec_tests:
        - test/integration/cassandra
    attributes:
      axonops:
        cassandra:
          version: '5.0.5'
          install_format: 'tar'
          tls:
            internode_encryption: 'all'
            type: 'jks'
          start_on_install: false
          wait_for_start: false
